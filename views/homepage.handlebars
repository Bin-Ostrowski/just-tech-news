<ol class="post-list">
    <!--with #each - Any HTML code in between (e.g., the <li> element) will be repeated for every item in posts-->
    {{#each posts as |post|}}
  <li>
    {{> post-info post }}
  </li>
  {{/each}}
</ol>